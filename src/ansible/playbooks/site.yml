---
# Master playbook — runs all migration phases in sequence
# Organization: Infinite Improbability Corp (IIC)
#
# Usage:
#   ansible-playbook playbooks/site.yml --ask-vault-pass -e @config/variables/variables.yml
#
# Run individual phases with tags:
#   ansible-playbook playbooks/site.yml --tags preflight
#   ansible-playbook playbooks/site.yml --tags veeam
#   ansible-playbook playbooks/site.yml --tags validate

- import_playbook: preflight.yml
- import_playbook: veeam-migration.yml
- import_playbook: hycu-migration.yml
- import_playbook: validate.yml
